# 帳票AI読取アプリ 連携仕様書(看護ポータルサイト向け)

作成日: 2026年8月17日
対象: 「看護師個人ポータル(マイページ)」制作担当者

---

## 1. これは何か

「帳票AI読取アプリ(shijisho-check-app)」は、訪問看護指示書・介護保険証・介護負担割合証・薬情(薬剤情報提供書)を、スタッフがスマホで撮影 → AIが自動判定・文字起こし → 内容確認 → 保存、まで行う社内ツールです。

保存されたデータは Cloudflare の D1(SQLite)・R2(写真保存)に入っており、看護ポータルサイト側から**読み取り専用API**で取得できるようにしてあります。書き込み(データの追加・修正・削除)はこのアプリ側からのみ行い、ポータル側からは行いません。

- アプリ本体(お試し版): https://yst-nakamura.github.io/shijisho-check-app/
- データ保存先: Cloudflare D1「shiten-toggo-db」内の `document_records` テーブル
- 写真保存先: Cloudflare R2「care-document-photos」
- API窓口: Cloudflare Worker「yakujou-records-api」(名前は薬情専用だった名残で、実際は全書類共通)

---

## 2. 今すぐ使えるAPI:期限アラート一覧

```
GET https://yakujou-records-api.tetsuya-nakamura-y.workers.dev/alerts
Authorization: Bearer <READ_SECRETの値>  ※値は中村さんに別途確認してください
```

### レスポンス例

```json
{
  "alerts": [
    {
      "riyousha_name": "山田太郎",
      "doc_type": "shijisho",
      "valid_until": "2026-08-20",
      "days_remaining": 2,
      "status": "yellow",
      "recorded_at": "2026-08-15T00:00:00.000Z"
    }
  ]
}
```

| フィールド | 内容 |
|---|---|
| `riyousha_name` | 利用者名(スタッフの手入力。※4章の注意点を参照) |
| `doc_type` | `shijisho`(指示書) / `hokensho`(介護保険証) / `futanwariai`(介護負担割合証) のいずれか。薬情(`yakujou`)には期限の概念がないため対象外 |
| `valid_until` | 有効期限(YYYY-MM-DD) |
| `days_remaining` | 今日から見た残り日数(マイナスは期限切れ) |
| `status` | `red`(期限切れ) / `yellow`(14日以内) / `green`(それ以外) |
| `recorded_at` | この記録が保存された日時(ISO8601) |

同じ利用者・同じ書類種別の記録が複数ある場合は、**最新の1件だけ**が返ります(履歴ではなく「現在の状態」を返す設計)。

mypage.jsx案の「お知らせ・アラート」欄(指示書期限アラート)は、このAPIでそのまま実装できます。

---

## 3. まだAPI化していないデータ(必要なら追加できます)

`document_records` テーブルには、上記の期限情報以外にも書類ごとの読み取り結果が全項目そのまま入っています(`extracted_json`列にJSON形式で保存)。現時点では`/alerts`が期限関連の項目しか返しませんが、他の項目が必要であれば、そのつど読み取り専用エンドポイントを追加できます。

参考までに、書類ごとに保存されている項目は以下の通りです。

**指示書(shijisho)**: 指示書の種類, 発行先ステーション名, 医療機関名/所在地/電話/FAX, 医師名, 交付日, 有効期間(開始/終了), 対象者氏名表記, 生年月日, 住所, 要介護度, 主たる傷病名, 病状・治療状態, 留意事項・指示事項, 装着医療機器, 重症心身障害児区分, 別表7・8該当, 他ステーションへの指示, 緊急時連絡先

**介護保険証(hokensho)**: 被保険者番号, 氏名, 生年月日, 住所, 交付年月日, 保険者番号/名, 要介護状態区分, 認定年月日, 認定有効期間(開始/終了)

**介護負担割合証(futanwariai)**: 被保険者番号, 氏名, 生年月日, 住所, 負担割合, 適用期間(開始/終了), 交付年月日, 保険者番号/名

**薬情(yakujou)**: 対象者氏名表記, 発行日, 発行薬局名, 薬剤一覧(1剤ごとに薬品名・用法用量・効能効果・注意事項をまとめて文字起こし)

また、各記録には元の撮影写真(R2)も紐づいています。写真そのものを見られるようにする場合も、専用の読み取り専用エンドポイントを追加できます(現状は内部確認用のエンドポイントのみで、外部への公開はしていません)。

**追加のAPIが必要になったら、中村さんに「◯◯の項目/写真も見られるようにしてほしい」と伝えてください。**中村さんの方でエンドポイントを追加し、この仕様書を更新します。

---

## 4. 連携する上での注意点

- **利用者の識別方法**: `riyousha_name`はスタッフがその場で手入力した氏名の文字列です。利用者IDのような一意なキーではないため、全角/半角スペース・旧字体などの表記ゆれで、ポータル側の利用者マスタと単純一致しない可能性があります。名寄せ方法(完全一致／部分一致／別途対応表を用意するなど)は、実装時に中村さんと相談してください。
- **書き込みは不可**: このAPIは読み取り専用です。ポータル側からデータを追加・修正することはできません(打刻データなど、ポータル固有のデータは別途ポータル側のDBで管理してください)。
- **合言葉(READ_SECRET)の扱い**: このAPI専用の合言葉です。書き込み用の合言葉(WRITE_SECRET)とは別物で、これだけでは書き込みはできません。ソースコードやチャット履歴にそのまま貼り付けず、環境変数などで安全に管理してください。
- **データの鮮度**: `/alerts`はリクエストのたびにD1へ直接問い合わせるため、常に最新の状態が返ります(キャッシュはしていません)。

---

## 5. 質問・追加リクエストの窓口

このAPI・データ構造についての質問や、追加で必要なエンドポイントのリクエストは、中村さんまでお願いします。
